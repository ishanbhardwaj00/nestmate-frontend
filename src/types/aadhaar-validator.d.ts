// types/aadhaar-validator.d.ts
declare module 'aadhaar-validator' {
  // Provide basic type information here if known, or just declare the module
  // Example (basic structure):
  export function isValidNumber(aadhaarNumber: string): boolean

  // For more complex modules, you may need to provide additional type declarations
}
